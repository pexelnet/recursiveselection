package eu.matejkormuth.recursiveselection;

import com.sk89q.worldedit.WorldEdit;
import org.bukkit.entity.Player;

public class WESupport {

    public static void setSelection(BoundingBox box, Player player) {
        WorldEdit.getInstance().getSession(player).getRegionSelector().
    }
}
